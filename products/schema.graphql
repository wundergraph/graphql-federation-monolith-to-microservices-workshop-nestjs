extend schema @link(url: "https://specs.apollo.dev/federation/v2.4", import: ["@override", "@key", "@shareable"])

type Query {
  getProducts: [Product]! @override(from: "monolith")
}

type Product {
  upc: ID! @override(from: "monolith")
  rating: Float @override(from: "monolith")
}
