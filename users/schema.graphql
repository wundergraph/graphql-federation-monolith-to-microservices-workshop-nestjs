extend schema @link(url: "https://specs.apollo.dev/federation/v2.4", import: ["@override", "@key", "@shareable"])

type Query {
  getUser(id: Int!): User @override(from: "monolith") @shareable
}

type User @key(fields: "id") {
  id: Int! @override(from: "monolith")
  username: String! @override(from: "monolith")
}
